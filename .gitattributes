# Git LFS - Large File Storage Configuration
# Use this if you decide to track large files in the future

# Blender files (if you decide to track them, use LFS)
# *.blend filter=lfs diff=lfs merge=lfs -text

# Media files (if needed in repo, use LFS)
# *.mp4 filter=lfs diff=lfs merge=lfs -text
# *.mov filter=lfs diff=lfs merge=lfs -text
# *.png filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text

# Prevent accidental commits of common secrets
*.env filter=private
*.key filter=private
*.pem filter=private
credentials.json filter=private

# Line ending normalization
* text=auto
*.py text eol=lf
*.sh text eol=lf
*.md text eol=lf
*.json text eol=lf
*.yaml text eol=lf
*.yml text eol=lf

# Binary files
*.blend binary
*.png binary
*.jpg binary
*.mp4 binary
*.mov binary
*.zip binary
